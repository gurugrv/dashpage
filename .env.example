# === Database (required) ===
DATABASE_URL="postgresql://builder:builder@localhost:5432/ai_builder"

# === Encryption secret for DB-stored API keys (required) ===
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KEYS_ENCRYPTION_SECRET=""

# === LLM Provider Keys (at least one required) ===
OPENROUTER_API_KEY=""     # Recommended: one key covers all models
ANTHROPIC_API_KEY=""      # Direct Anthropic access
GOOGLE_GENERATIVE_AI_API_KEY=""  # Google Gemini
OPENAI_API_KEY=""         # OpenAI direct
DEEPINFRA_API_KEY=""      # DeepInfra - open-source models (Llama, DeepSeek, Qwen, Mixtral)
ZAI_API_KEY=""            # Z.ai (Zhipu AI) - GLM models

# === Image API ===
PEXELS_API_KEY=""             # Pexels API key for searchImages tool (https://www.pexels.com/api/)

# === Search API (optional - enables webSearch tool) ===
BRAVE_SEARCH_API_KEY=""      # Brave Search API key (https://api-dashboard.search.brave.com/)
TAVILY_API_KEY=""             # Tavily Search API key - fallback (https://tavily.com/)

# === Debug (optional) ===
DEBUG_AI_STREAM_OUTPUT="false"  # true|1 to print live streamed model output in server console
